#include <stdio.h>

/**
  * main - Entry point
  *
  * Description: prints the specified string to the screen.
  * Return: zero
  */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,with proper
grammar, but the outcome is a piece of art,\n");
return (0);
}
