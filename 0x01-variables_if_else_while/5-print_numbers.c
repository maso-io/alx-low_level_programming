#include <stdio.h>

/**
  * main - prints out the numbers to 10
  *
  * Return: 0 (Success)
  *
  */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');

	return (0);
}
